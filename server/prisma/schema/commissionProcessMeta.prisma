model CommissionProcessMeta {
  id              String   @id @default("commission-meta") // only one row
  lastProcessedAt DateTime
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
